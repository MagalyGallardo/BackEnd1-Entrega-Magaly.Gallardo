<h1>Carrito de Productos</h1>
{{#if cart.length}}
<ul>
    {{#each cart}}
    <li>
        <h2>{{this.title}}</h2>
        <p>Precio: {{this.price}}</p>
        <p>Cantidad: {{this.quantity}}</p>
    </li>
    {{/each}}
</ul>
{{else}}
<p>El carrito está vacío</p>
{{/if}}
